import { styled } from 'styled-components';

export const IngredientForm = styled.form`
  margin-top: 3rem;
  display: flex;
  flex-direction: column;
  gap: 3rem;

  > div {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }
`;
